{
  "name": "site",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "deploy": "rm -rf out && next build && next export && gh-pages -d out -t true"
  },
  "dependencies": {
    "next": "10.0.1",
    "pretty-ms": "^7.0.1",
    "react": "17.0.1",
    "react-dom": "17.0.1"
  },
  "devDependencies": {
    "@types/node": "^14.14.7",
    "@types/react": "^16.9.56",
    "gh-pages": "^3.1.0",
    "typescript": "^4.0.5"
  }
}
